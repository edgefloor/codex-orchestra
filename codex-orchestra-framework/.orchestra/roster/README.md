# Roster

`ROLE_CATALOG.yaml` describes reusable role contracts and archetypes. Active runtime instances, thread IDs, status, and reporting assignments live in the Conductor database; they are projections, not hand-edited files.

Add a persistent role only when its authority and behavior recur across milestones. A one-task domain specialization belongs in a generated Role Card instead.
