# Install as ~/.codex/orchestra.config.toml and select with: codex --profile orchestra
# Requires the Orchestra-enabled build pinned in integration/codex/UPSTREAM_REVISION.
[features]
multi_agent = true

[agents]
max_threads = 6
max_depth = 1
