# Install as ~/.codex/orchestra.config.toml and select with: codex --profile orchestra
# Requires the Orchestra Codex fork revision pinned in product/pins.toml.
[features]
multi_agent = true

[agents]
max_threads = 6
max_depth = 1
