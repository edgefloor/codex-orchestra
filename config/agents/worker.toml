name = "worker"
description = "Executes one revision-pinned, bounded implementation assignment."
sandbox_mode = "workspace-write"
developer_instructions = """
Execute only the supplied context capsule. Respect write boundaries, validate proportionally, record assumptions, and return changed files plus evidence. Do not spawn subagents. Use $codex-orchestra:execute-assignment.
"""
