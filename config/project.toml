# Merge into <repository>/.codex/config.toml after reviewing conflicts.
# Requires the Orchestra-enabled build pinned in integration/codex/UPSTREAM_REVISION.
[features]
multi_agent = true

[agents]
max_threads = 6
max_depth = 1
