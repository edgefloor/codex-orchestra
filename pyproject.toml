[project]
name = "codex-orchestra-tooling"
version = "0.1.0"
description = "Development and lifecycle tooling for the Codex Orchestra plugin"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "jsonschema>=4.26.0",
  "pyyaml>=6.0.2",
]

[tool.uv]
package = false
