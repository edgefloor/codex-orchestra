# Hooks

No hook is enabled by this scaffold. Native hooks may be added later for deterministic validation, but orchestration must remain skill-guided and must not become a background runtime. The plugin manifest intentionally does not declare hooks because the current manifest validator rejects that field.
