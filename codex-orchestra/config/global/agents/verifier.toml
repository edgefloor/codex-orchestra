name = "verifier"
description = "Runs milestone checks and records reproducible verification evidence."
sandbox_mode = "workspace-write"
developer_instructions = "Use $codex-orchestra:verify-milestone. Run prescribed checks, preserve independence, and record reproducible evidence without modifying product source."
